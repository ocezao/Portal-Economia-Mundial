/**
 * Loading Admin Dashboard
 * Componente de loading para o painel administrativo
 */

import { DashboardSkeleton } from "@/components/skeletons";

export default function Loading() {
  return <DashboardSkeleton />;
}
